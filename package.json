{
	"name": "signed-storage",
	"version": "1.0.0",
	"private": true,
	"type": "module",
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"config": "node src/utils/config-generator.cjs",
		"test": "vitest",
		"test:quick": "node src/utils/testing/cli.js --quick",
		"test:health": "node src/utils/testing/cli.js --health",
		"test:full": "node src/utils/testing/cli.js --full",
		"test:help": "node src/utils/testing/cli.js --help"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.7.5",
		"vitest": "~3.0.7",
		"wrangler": "^3.114.0"
	},
	"dependencies": {
		"aws4fetch": "^1.0.20"
	}
}
